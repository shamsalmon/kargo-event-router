---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: messagechannels.kargo-event-router.io
spec:
  group: kargo-event-router.io
  names:
    kind: MessageChannel
    listKind: MessageChannelList
    plural: messagechannels
    singular: messagechannel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MessageChannel describes a destination to which events can be delivered.
          EventRouters reference MessageChannels by name.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec describes the destination.
            properties:
              slack:
                description: |-
                  Slack describes a Slack destination to which events are delivered as
                  messages. Exactly one of Webhook or Slack must be set.
                properties:
                  channel:
                    description: Channel is the channel ID or name (e.g. `#deployments`)
                      to post to.
                    minLength: 1
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references a Secret in the same namespace as the
                      MessageChannel. The Secret's data map must contain a `token` key whose
                      value is a Slack bot token with the `chat:write` scope. The bot must
                      be a member of the targeted channel. One token can be shared by any
                      number of MessageChannels.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - channel
                - secretRef
                type: object
              webhook:
                description: |-
                  Webhook describes a webhook destination to which events are delivered
                  as CloudEvents. Exactly one of Webhook or Slack must be set.
                properties:
                  secretRef:
                    description: |-
                      SecretRef references a Secret in the same namespace as the
                      MessageChannel. When specified, the Secret's data map must contain a
                      `secret` key whose value is used to compute an HMAC-SHA256 signature
                      of each request body. The signature is sent in the
                      X-Kargo-Event-Router-Signature header so receivers can verify the
                      authenticity of the payload.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  url:
                    description: URL is the address to which events are POSTed.
                    pattern: ^https?://
                    type: string
                required:
                - url
                type: object
            type: object
            x-kubernetes-validations:
            - message: exactly one of webhook or slack must be set
              rule: has(self.webhook) != has(self.slack)
        type: object
    served: true
    storage: true
    subresources: {}
